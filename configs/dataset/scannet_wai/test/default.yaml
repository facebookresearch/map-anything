dataset_str:
  "ScanNetWAI(
    split='${dataset.scannet_wai.test.split}',
    resolution=${dataset.scannet_wai.test.dataset_resolution},
    principal_point_centered=${dataset.scannet_wai.test.principal_point_centered},
    seed=${dataset.scannet_wai.test.seed},
    transform='${dataset.scannet_wai.test.transform}',
    data_norm_type='${dataset.scannet_wai.test.data_norm_type}',
    ROOT='${dataset.scannet_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.scannet_wai.test.dataset_metadata_dir}',
    variable_num_views=${dataset.scannet_wai.test.variable_num_views},
    num_views=${dataset.scannet_wai.test.num_views},
    covisibility_thres=${dataset.scannet_wai.test.covisibility_thres})"
split: 'test'
dataset_resolution: ${dataset.resolution_test_scannet}
principal_point_centered: ${dataset.principal_point_centered}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/scannetv2
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
variable_num_views: ${dataset.test.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: 0.25
